# Configurador de Tableros PYRE

Sistema web para PYRE (integrador ABB) que administra el catálogo de componentes, arma tableros seccionables (interruptor principal + salidas), genera BOM y calcula precios. Plazo: ~30 días desde 2026-07-16.

**Antes de tocar código, leé en este orden:**

1. `docs/README.md` — cómo levantar el stack local (Docker, puertos, login de prueba).
2. `docs/superpowers/specs/2026-07-16-configurador-tableros-design.md` — diseño general del sistema (todas las fases).
3. `docs/superpowers/specs/2026-07-16-fase-b-catalogo-design.md` — diseño del catálogo (Fase B).
4. `docs/superpowers/plans/*.md` — planes de implementación ya ejecutados, con notas inline (`> **Note:**`) documentando bugs/gotchas reales encontrados durante la implementación (no son solo planes, son bitácora técnica).
5. `docs/diccionario_datos.md` y `docs/reglas_negocio.md` — qué significa cada tabla/columna y las reglas de negocio vigentes. Se actualizan a medida que avanzan las fases.
6. `docs/consultas_ingenieria.md` — preguntas de negocio/eléctricas abiertas que necesitan confirmación de un ingeniero de PYRE, no adivinar.

## Estado (actualizar al final de cada fase)

- ✅ **Fase A — Fundaciones**: Docker Compose, auth (analista/supervisor), modelo de datos base, login React funcionando. Mergeada a `master`.
- ✅ **Fase B — Catálogo**: importador de Excel ABB (jerarquía completa; el Excel tiene 10.247 filas de datos pero ~9.062 son componentes reales, el resto son filas de sección con "Codigo SAP" en blanco) + otros materiales, con historial de precios y auditoría. Mergeada a `master`.
- 🟨 **Fase C — Motor de configuración + BOM + esquema visual**: ciclos 1 (backend del motor: modelo de datos, reglas de cálculo, API REST), 2 (UI mínima: proyectos/tableros/secciones/salidas, esquema visual simple, edición post-creación de nivel de falla e interruptor principal, formato tripolar, validación de amperios enteros), 3 (poblar `atributos` del catálogo ABB real por regex sobre `categoria_path`/`descripcion` — ~64% de cobertura en categorías en-alcance, ver `docs/consultas_ingenieria.md` #1 para el resto), 4 (búsqueda de catálogo operativa a escala real: búsqueda por `codigo`/`codigo_comercial`/`descripcion` con ranking de relevancia, índices GIN trigram (`pg_trgm`), lookup de existentes por lotes para evitar el límite de profundidad de Postgres en imports grandes, columna `codigo_comercial` ampliada a 200 caracteres, y el fix de filas fantasma con "Codigo SAP" en blanco — todo verificado de punta a punta reimportando el catálogo real de ABB), 5 (rediseño de UI con Tailwind + modelador gráfico del esquema visual: shell de navegación, esquema visual con estética blueprint y patrón diagonal para diferenciales en vez de un segundo color, zoom y capas funcionales, workspace de proyecto con pestañas por tablero y estado de vista persistido por tablero, grilla de tarjetas + modal en Proyectos, tabla de salidas con badges de estado — spec en `docs/superpowers/specs/2026-07-17-rediseno-ui-esquema-visual-design.md`), 6 (CRUD completo con borrado en cascada manual en los 4 niveles — proyecto/tablero/fila/salida, con `ConfirmDialog` reutilizable y superficie de error visible en cada confirmación; reestructuración conceptual "Sección" → "Fila" con el interruptor principal como primera pestaña no borrable; buscador de catálogo (`ComponentePicker`) convertido en modal autocontenido de ~700px con scroll interno acotado y filtro maestro `categorias` obligatorio, resolviendo el bug de scroll de página completa; pulido visual — "Nivel de falla" renombrado a "Intensidad de Cortocircuito (Icc)", íconos de gestión neutros con hover rojo — spec en `docs/superpowers/specs/2026-07-18-workspace-crud-filas-design.md`) mergeados a `master`. Ciclo 7 (búsqueda del picker de interruptores scoped con `solo_con_atributos` — excluye accesorios sin `atributos` extraídos del mismo `categoria_raiz`, sin tocar el parser; filtros estructurados de polos/corriente nominal/capacidad de corte en `/catalogo/buscar` + nuevo endpoint `GET /catalogo/opciones-filtro` con valores realmente presentes en el catálogo, no hardcodeados; panel de filtros colapsable en `ComponentePicker` con chips removibles; `SalidaResponse`/`TableroResponse` devuelven `componente_codigo`/`componente_codigo_comercial` legibles en vez de solo el UUID, consumidos en `SeccionBlock` y `DetalleTablero`; hook compartido `useCerrarAlClickFuera` (nuevo `frontend/src/hooks/`) que corrige el cierre accidental de los ~9 modales de la app cuando una selección de texto con drag termina fuera del modal — bug de resolución de eventos del navegador, no de la app — spec en `docs/superpowers/specs/2026-07-19-catalogo-busqueda-filtros-display-design.md`, plan en `docs/superpowers/plans/2026-07-19-catalogo-busqueda-filtros-display-implementation-plan.md`) mergeado a `master` (la rama `feat/fase-c-catalogo-filtros-display` ya fue integrada y eliminada). Ciclo 8 (hardening post-auditoría: filtros del motor de propuesta movidos a SQL con operadores JSONB — antes traía todo el catálogo a memoria por salida; `passlib` abandonado reemplazado por `bcrypt` directo con test de regresión de hashes legacy; guard-rail que impide arrancar en `production` con secretos default; validación de upload de Excel con tope de 20 MB + magic bytes ZIP; autorización por propiedad en todos los endpoints — analista solo sus proyectos y anidados, supervisor todo, reasignación vía `analista_id` exclusiva del supervisor; y fix de infra de tests: `catalogo_componente` se trunca por test en `conftest.py`, la fragilidad de orden alfabético entre archivos de test quedó eliminada — spec en `docs/superpowers/specs/2026-07-19-fase-c-hardening-seguridad-performance-design.md`, plan en `docs/superpowers/plans/2026-07-19-fase-c-hardening-implementation-plan.md`) completo en la rama `feat/fase-c-hardening`, pendiente de merge a `master`. Specs: `docs/superpowers/specs/2026-07-16-fase-c-motor-configuracion-design.md`, `docs/superpowers/specs/2026-07-16-fase-c-ui-configurador-design.md`, `docs/superpowers/specs/2026-07-16-catalogo-abb-atributos-design.md`. Falta: BOM.
- ⬜ **Pista B — Agente de extracción CAD/PDF**: no arrancada (pensada para correr en paralelo a la pista principal). Gotcha real encontrado al probar conversión de PDF→markdown con `pymupdf4llm` sobre documentos reales de proyecto (`project/`, gitignored): páginas con una cantidad muy alta de trazos vectoriales (sello oficial, plano técnico detallado — un caso real tenía ~62.000) hacen que el motor de detección de tablas/layout de `pymupdf4llm` se cuelgue (tardó ~40 min en una página vs. ~0.2s normal). Mitigación que funcionó: contar `page.get_drawings()` por página y, si supera un umbral (~3000), usar extracción de texto plano (`page.get_text()`) en vez del pipeline de layout para esa página puntual. Tenerlo en cuenta al diseñar el pipeline de extracción de Pista B — sea cual sea la librería, va a necesitar un guard-rail similar para no colgarse con planos/sellos reales.
- ⬜ **Fase D/E — Precios/mano de obra + exportables + UI pulida + hardening**: no arrancadas.

## Convenciones del proyecto

- Backend: Python/FastAPI + SQLAlchemy 2.0 (`Mapped[]`/`mapped_column()`) + Alembic + PostgreSQL. Frontend: React/TypeScript + Vite + Vitest.
- TDD siempre: test que falla → implementación mínima → test pasa → commit.
- JSON en Postgres siempre como `JSONB` (no `JSON` genérico) — necesario para indexar/consultar más adelante.
- `venv` del backend en `backend/venv/` (no está en git). `docker compose up -d db` antes de correr tests.
- Ramas de feature por fase (`feat/fase-x-...`), merge local a `master` al terminar, con los dos suites de test en verde.
- Un archivo Excel confidencial de PYRE (`samples/catalogo/TABLA POLOS TABLEROS SECC. CON CAMBIOS.xlsx`) es la herramienta interna actual de costeo — el usuario pidió explícitamente no analizarlo todavía ("lo analizaremos luego"). No lo abras sin que te lo pidan.
- `samples/catalogo/` tiene archivos reales de proveedores (gitignored, confidenciales) — útiles para verificar parsers contra datos reales antes de dar una fase por terminada.
